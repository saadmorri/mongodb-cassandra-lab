
---

## `docker-compose.yml`

```yaml
version: "3.8"

services:
  mongodb:
    image: mongo:7
    container_name: mongodb_lab
    ports:
      - "27017:27017"

  cassandra:
    image: cassandra:4.1
    container_name: cassandra_lab
    ports:
      - "9042:9042"